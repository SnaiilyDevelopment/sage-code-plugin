# Sage Architecture — Auto Map

> Generated by `scripts/context/repo-map.py`. Repo evidence wins over memory.
> Repo: `C:\Users\SageOS\Documents\GitHub\sage-tweaks` | Version: `2.2.0` | Branch: `main`

## Product
- **SageTweaks** (`com.sagetweaks.desktop`) — bundle targets: `nsis`
- External binaries: `binaries/presentmon, binaries/smartctl, binaries/iperf3, binaries/diskspd`
- Autogenerated permissions: `431` toml files

## Build / Test Commands (from package.json)
- `build`: `node scripts/run-node-bin.cjs vite/bin/vite.js build`
- `tauri`: `tauri`
- `tauri:dev`: `tauri dev`
- `tauri:dev:env`: `node scripts/tauri-dev-env.cjs`
- `tauri:build`: `node scripts/tauri-build-safe.cjs`
- `tauri:build:verify`: `pnpm tauri:build && node scripts/verify-tauri-bundle.cjs`
- `tauri:build:release`: `node scripts/tauri-build-release.cjs`
- `test`: `node scripts/run-vitest-safe.cjs`
- `test:ui`: `node scripts/run-vitest-safe.cjs --ui`
- `lint`: `node scripts/run-node-bin.cjs eslint/bin/eslint.js src --ext ts,tsx`
- `security:policy`: `node scripts/security-policy-check.mjs`
- `wiring:check`: `node scripts/command-wiring-check.mjs --self-test && node scripts/command-wiring-check.mjs`
- `ipc:check`: `node scripts/ipc-boundary-check.mjs`
- `verify:updater`: `node scripts/verify-updater-production.mjs`
- `security:cert`: `node scripts/check-pinned-api-cert.cjs`
- `type-check`: `node scripts/run-node-bin.cjs typescript/bin/tsc --noEmit`
- `build:game-catalog`: `node scripts/build-game-catalog.mjs`

## Entry Points
- src/main.tsx, src/App.tsx, src-tauri/src/main.rs, src-tauri/src/lib.rs

## Tweak Modules (sample from src-tauri/src/tweaks/mod.rs)
- backup, command_revert, impls, profiles, report, scheduler, types, utils

## Dependencies
- deps: @radix-ui/react-dialog, @radix-ui/react-dropdown-menu, @radix-ui/react-slot, @radix-ui/react-switch, @radix-ui/react-tooltip, @tailwindcss/vite, @tanstack/react-query, @tauri-apps/plugin-store, canvas-confetti, class-variance-authority
- dev: @tauri-apps/api, @tauri-apps/cli, @testing-library/dom, @testing-library/jest-dom, @testing-library/react

## Tauri Config
- bundle: ['nsis']
- externalBin: ['binaries/presentmon', 'binaries/smartctl', 'binaries/iperf3', 'binaries/diskspd']
- updater: True
- windows code: src-tauri/src/tweaks, src-tauri/src/system, src-tauri/src/firmware

## Important Config
- src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src-tauri/capabilities/default.json, package.json, AGENTS.md, CLAUDE.md

## Frequently Modified (last 100 commits)
- 
src-tauri/src/lib.rs (20)- src-tauri/permissions/default.toml (13)- src/lib/tauri.ts (11)- src-tauri/src/tests/security_tests.rs (10)- .wolf/anatomy.md (10)- src/lib/routes.ts (9)- src-tauri/src/module_tweak_runtime/tests.rs (8)- src/App.tsx (8)

## Tests
- {'vitest_files': 181, 'rust_files': 283, 'qa_vm': True}

## Key Paths
- Frontend: `src/` (React 19, Vite, Zustand, TanStack Query)
- Backend: `src-tauri/src/` (Rust, Tauri v2, commands/, tweaks/impls/, security/, gaming/, module_tweak_runtime/)
- Config: `src-tauri/tauri.conf.json` (perMachine NSIS, updater endpoints)
- Capabilities: `src-tauri/capabilities/default.json` → `permissions/default.toml` → `permissions/autogenerated/*.toml`
- QA: `qa/vm-matrix/`, `scripts/*check.mjs`

## Verification Quick Map
- Frontend: `pnpm type-check` → `pnpm lint` → `pnpm test`
- Rust/Tauri: `cargo check` / `cargo test` (in src-tauri) → `pnpm wiring:check` → `pnpm ipc:check` → `pnpm security:policy`
- Windows: validate registry path + rollback snapshot before apply

## Change Detection
- Map hash: `b362cafa93a2 ` — re-run repo-map only if `git diff --stat` or `package.json`/`tauri.conf.json` changed.

## Conventions (CLAUDE.md preview)
```
# CLAUDE.md — SageTweaks Engineering Playbook

**why** lives here. `AGENTS.md` is canonical **what** (commands, exact
implementation rules) — read every session. `.claude/rules/*.md` are
auto-loaded **how** (exact function names, CI-checked invariants). A fact
lives in exactly one of the three; this file cross-references, never copies.

Load order: `AGENTS.md` → `.wolf/anatomy.md` → `.wolf/cerebrum.md` →
`.wolf/memory.md` (continuing prior work) → `docs/agent-context.md` (on
demand).

## Project Overview

A Tauri v2 shell around a large catalog of Windows registry/service/power
tweaks, sold as a licensed product. One node in a two-repo product — this
repo is the client; billing/accounts/mark
```

## Known Pitfalls
- `src-tauri/.cargo/config.toml` [profile.release] was removed 2026-08-19 — Cargo.toml is authoritative.
- `bundle.windows.wix` block dead — only `nsis` target runs.
- Never invent registry paths / service names — verify via local tooling or docs.
- Performance claims require `hypothesis → measured → compare` with baseline.
